{
	"options": {
		"foo": {
			"matches": ["f"],
			"accepts_arguments": true,
			"placeholder": "BAR"
		}
	}

}
/**
   Basic help handling with a single short option that does accept
   arguments.

   Notice that, by default, the option placeholder is the option name in
   upper case.

   @expects

Usage: progname [-f BAR]

Options and arguments:

  -f BAR                    (No help text for this option.)
*/
